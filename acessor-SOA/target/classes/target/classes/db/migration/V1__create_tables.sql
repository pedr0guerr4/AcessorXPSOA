-- Tabela exatamente com o mesmo nome da entidade: @Table(name = 'ativos')
create table if not exists ativos (
  id bigint generated by default as identity primary key,
  nome varchar(120) not null,
  classe varchar(20) not null,
  retorno_historico double not null,
  liquidez_dias int not null
);

-- índices opcionais
create index if not exists idx_ativos_nome on ativos(nome);
